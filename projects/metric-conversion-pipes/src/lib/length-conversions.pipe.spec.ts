import { LengthConversionsPipe } from './length-conversions.pipe';

describe('LengthConversionsPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthConversionsPipe();
    expect(pipe).toBeTruthy();
  });
});
