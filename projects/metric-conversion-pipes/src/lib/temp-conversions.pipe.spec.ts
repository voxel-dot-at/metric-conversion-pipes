import { TempConversionsPipe } from './temp-conversions.pipe';

describe('TempConversionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TempConversionsPipe();
    expect(pipe).toBeTruthy();
  });
});
