import { MassConversionsPipe } from './mass-conversions.pipe';

describe('MassConversionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MassConversionsPipe();
    expect(pipe).toBeTruthy();
  });
});
